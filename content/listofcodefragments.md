## List of code fragments
{:.no-label-increment}

- **Lst. 1:** A media listen event represented as an RDF triple in Turtle syntax, using schema.org vocabulary. Own work.
- **Lst. 2:** A listen event and a watch event from two different platforms, represented in the same RDF structure using schema.org vocabulary. Own work.
- **Lst. 3:** A WAC policy granting Alice read access and Bob full control over a resource in Bob's pod, using URI-identified agents and explicit permission modes. Own work.
- **Lst. 4:** An ACP policy granting read access to a resource only when the request comes from a specific agent using a specific client application, combining both conditions through a matcher. Own work.
- **Lst. 5:** GraphQL SDL schema for the `music-tracker` slice, combining schema.org and Music Ontology vocabularies. Own work.
- **Lst. 6:** GraphQL SDL schema for the `video-tracker` slice, using schema.org vocabulary throughout. Own work.
- **Lst. 7:** GraphQL SDL schema for the `cross-service-profile` slice, introducing the `csp_Creator` type with user-confirmed identity links, aggregated interaction statistics, and the `csp_UserInsight` type for user-validated behavioural assertions. Own work.
- **Lst. 8:** Bootstrap configuration declaring OpenFGA relationships for the `music-tracker-client` service account. Own work.
- **Lst. 9:** A `schema:ListenAction` event in JSON-LD format, linking a user agent to a MusicBrainz recording URI with a deterministic subject URI. Own work.
- **Lst. 10:** Delete-then-insert pattern for updating `schema:InteractionCounter` triples in Kvasir. Own work.
- **Lst. 11:** GraphQL query against the `music-tracker` slice returning artists ordered by interaction count. Own work.