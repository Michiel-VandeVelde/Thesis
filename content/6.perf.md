## Performance analysis
{:#perf}



In this section, we evaluate the execution-time performance of Traqula, comparing its different parsing modes,
contrasting it with [SPARQL.JS](cite:cites sparqljs) / [SPARQLAlgebra.JS](cite:cites sparqlalgebrajs), and highlighting the impact of parser reuse.
All measurements were conducted on Fedora Linux 43 (Workstation Edition), equipped with an 'Intel® Core™ Ultra 7 165U × 14' processor, having 16 GiB of RAM.
The execution times reported correspond to parsing a collection of 199 SPARQL 1.1 queries into the respective targets.

[](#traqula-bench) shows the execution times of Traqula across different targets and parser configurations.
Patching a modular SPARQL 1.1 parser to support SPARQL 1.2 results in a small but statistically significant increase in execution time (p < 0.05),
with a mean increase of 6.13% <!-- 5.71/5.38 -->.
Tracking source information to support round-tripping introduces additional overhead due to the more complex lexing process,
with mean increases of  16.5% <!-- 6.27/5.38 -->
and 17.5% <!-- 6.71/5.71 --> for SPARQL 1.1 and 1.2 parsing respectively (both p < 0.05).
Parsing directly into algebra, effectively performing an AST transformation after parsing, further increases execution time,
with mean increases of 58.7% <!-- 8.54/5.38 --> for SPARQL 1.1 and 63.4% <!-- 9.33/5.71 --> for SPARQL 1.2 (p < 0.05).

Despite these relative increases, [](#traqula-sparqljs) shows Traqula remains substantially faster than existing solutions.
The mean execution time for parsing into an AST using Traqula is 5.38 ms, compared to 38.17 ms for SPARQL.JS, a 709.5% difference,
even though Traqula generates a more complete and correct AST.
Parsing into algebra shows a similar trend: Traqula takes 8.54 ms, while SPARQLAlgebra.js (using SPARQL.JS) requires 39.69 ms, a 464.8% difference.
Much of this advantage can be attributed to the performance of Chevrotain, the parser toolkit underlying Traqula.


<figure id="traqula-bench">
<img src="img/boxplots.svg" alt="Traqula bench">
<figcaption markdown="block">
Execution time (ms) comparison of Traqula's SPARQL 1.1 and 1.2 parsers parsing 199 SPARQL 1.1 queries into different targets.
The small differences between the 1.1 parser and the 1.2 parser, which is constructed as a modification of the 1.1 parser,
can likely be attributed to the increased complexity of the lexer and grammar.
Larger differences arise from the parser target:
parsing to an AST is fastest, parsing to an AST with source location tracking is slightly slower, and parsing directly to algebra is the slowest.
</figcaption>
</figure>

<figure id="traqula-sparqljs">
<img src="img/barplot_means.svg" alt="Traqula bench">
<figcaption markdown="block">
Execution time (ms) comparison between Traqula and
[SPARQL.JS](cite:cites sparqljs) / [SPARQLAlgebra.JS](cite:cites sparqlalgebrajs) when parsing 199 SPARQL 1.1 queries.
Parsing to both an AST (green) and to algebra (orange) is significantly faster using Traqula.
</figcaption>
</figure>


Finally, it is important to note the cost associated with parser creation in a non-compiled language like TypeScript.
Traqula optimizes the parser at runtime, making parser construction computationally expensive.
To illustrate, parsing the 199 queries with a prebuilt parser has a mean execution time of 5.38 ms,
whereas creating a new parser for each query results in a mean execution time of 2,572.03 ms; a slowdown of 478 times.
This underscores the importance of reusing parser instances wherever possible.
